package uebungen;

import java.io.*;
import java.nio.charset.StandardCharsets;

public class CSV_DateinLesen {

   public static void main(String[] args) {
       /*
        File file = new File("C:\\Users\\abdul.alsabbagh\\Documents\\GitHub\\prog2\\src\\uebungen\\csv_test.csv");
        try(LineNumberReader reader = new LineNumberReader(new InputStreamReader(new FileInputStream(file), StandardCharsets.UTF_8.name()))){
            while(reader.readLine() != null){
                if(reader.getLineNumber() > 1){
                    String[] fields = (reader.readLine()).split(";");


                }
            }
        }
        catch (IOException ex){
            System.err.println("error");
        }

        */
    }
}
